name: Cat Facts Bot

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Install dependencies
        run: |
          pip install requests==2.31.0 telethon==1.28.5

      - name: Run the bot
        run: |
          cd catfact
          python cat.py

